import { Container } from '@mui/material';

const Login = () => {

    return <Container maxWidth="xl">
        This is the login page, possibly just a redirect to shibboleth login. tbd
    </Container>;
};

export default Login;